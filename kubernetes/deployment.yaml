apiVersion: apps/v1
kind: Deployment
metadata:
  name: stravaboards
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: deployment-default-stravaboards
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: stravaboards
      annotations:
        deploy-date: "deploy-date-value"